Find the MIME type for the file:
xdg-mime query filetype filename.afdesign

Update the MIME database:
update-mime-database ~/.local/share/mime

Set the default application for your new MIME type:
xdg-mime default <affinity_app_name>.desktop application/x-affinity
